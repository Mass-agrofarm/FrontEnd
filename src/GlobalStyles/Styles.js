// color variables
const richBlack = `hsla(180, 58%, 4%, 1)`

const forestGreen = `hsla(132, 58%, 37%, 1)`
const pigmentGreen = `hsla(135, 59%, 41%, 1)`

const gunMetal = `hsla(180, 13%, 20%, 1)`
const dimGray = `hsla(175, 5%, 40%, 1)`
const cadetGray = `hsla(165, 4%, 60%, 1)`
const silver = `hsla(156, 5%, 80%, 1)`

const platinum = `hsla(135, 8%, 90%, 1)`
const whiteSmoke = `hsla(120, 11%, 95%, 1)`
const babyPowder = `hsla(120, 100%, 99%, 1)`



export const Colors = {
    "rich-black": richBlack,
    "forest-green": forestGreen,
    "pigment-green": pigmentGreen,
    "gunmetal": gunMetal,
    "dim-gray": dimGray,
    "cadet-gray": cadetGray,
    "silver": silver,
    "platinum": platinum,
    "white-smoke": whiteSmoke,
    "baby-powder": babyPowder,

// sectioning.
    'primary': richBlack,

    'accent-100': forestGreen,
    'accent-200': pigmentGreen,

    'background-100': babyPowder,
    'background-200': whiteSmoke,
    'background-300': platinum,

    'text-100': gunMetal,
    'text-200': dimGray,
    'text-300': cadetGray,
}